node_modules/
tmp/
*.log
.env
*.code-workspace

# Publish tokens must never be committed.
tokens.json
# Local seed overlays (the tracked seed-index.json is the canonical seed).
seed-index.local.json

# Runtime registry state lives on the Docker volumes, never in git.
data/
packages/
